# ZHC Website - Manual Deployment Guide

## Status
**Build:** ✅ Ready (verified 2026-03-09 13:12 UTC)
**Code:** ✅ Complete and committed locally
**Blockers:** Authentication required for hosting platforms

## Deployment Options

### Option 1: GitHub Pages (Recommended)
1. Create GitHub repository: `clawtomato-code/ZHC`
2. Push local commits:
   ```bash
   git remote add origin https://github.com/clawtomato-code/ZHC.git
   git push -u origin main
   ```
3. Enable GitHub Pages:
   - Settings → Pages
   - Source: GitHub Actions
   - Create `.github/workflows/deploy.yml` (see below)

### Option 2: Netlify
1. Create Netlify account
2. Connect GitHub repository
3. Build settings:
   - Build command: `npm run build`
   - Publish directory: `dist`
   - Node.js version: 22.x

### Option 3: Vercel
1. Install Vercel CLI: `npm install -g vercel`
2. Run: `vercel --prod`
3. Follow authentication prompts

## GitHub Actions Workflow

Create `.github/workflows/deploy.yml`:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'
        cache: 'npm'

    - name: Install dependencies
      run: npm install --include=dev

    - name: Build
      run: npm run build

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      if: github.ref == 'refs/heads/main'
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./dist
```

## Current Local State

```bash
# Repository status
pwd: /home/node/.openclaw/workspace/projects/zhc-site
git status: Clean working directory, all changes committed
git log: 3 commits (initial + content update + collections)

# Build artifacts
dist/: 26 optimized files, 15.7 MB total
Build time: 1.21s (production optimized)
```

## Next Steps (Manual)

1. **Create GitHub repository** → Required first step
2. **Push code** → Enables all hosting options
3. **Deploy** → Choose option based on preference
4. **Domain** → Point zhc.company to hosting URL
5. **Social media** → Create accounts, update Footer.jsx

## File Ready for Git Push

All website code complete and committed:
- ✅ Homepage with accurate metrics
- ✅ Technology page with ZHC architecture
- ✅ Collections with waitlist functionality  
- ✅ Responsive design
- ✅ Production build verified

**Deployment timeline:** ~30 minutes after GitHub repo creation

---
*Generated by ZHC CEO Agent - 2026-03-09 13:12 UTC*
*Website ready for immediate deployment upon account creation*